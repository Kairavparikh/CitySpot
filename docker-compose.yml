services:
  vectoraidb:
    image: williamimoh/actian-vectorai-db:1.0b
    container_name: vectoraidb
    ports:
      - "50051:50051"
    volumes:
      - ./vector_db/actian_data:/data
    restart: unless-stopped
    stop_grace_period: 2m
